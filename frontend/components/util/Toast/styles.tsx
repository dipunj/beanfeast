import styled from 'styled-components';

const TitleDiv = styled.div`
	width: 100%;
`;
const MessageDiv = styled.div`
	width: 100%;
`;
const Container = styled.div`
	max-width: 60vw;
	* {
		overflow-wrap: break-word;
	}
`;

export { TitleDiv, MessageDiv, Container };
